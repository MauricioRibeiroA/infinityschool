#start the virtuual env created
source myenv/bin/activate

#mysql bibliotecas dando problema com python 3.10 global (so roda no myenv)
#matplotlib dando problema com a versão 3.10 global (so roda no myenv)

#start anaconda terminal on linux terminal
conda list
#start anaconda navigator inside conda terminar
anaconda-navigator

#how to access mysql from terminal
mysql -u root -p

sudo apt-get install python3.8-tk

PYTEST
comando para instalar o pytest 
pip install pytest
o arquivo de teste deve iniciar com o nome test_
comando para rodar o teste
python -m pytest nome do arquivo onde estao os testes

